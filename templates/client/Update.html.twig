{% extends 'base-back.html.twig' %}

{% block body %}
    <h1> Update Client</h1>
    {{ form_start(f,{'attr': {'novalidate': 'novalidate'}} ) }}
    {{ form_widget(f) }}
    {{ form_end(f) }}
{% endblock %}