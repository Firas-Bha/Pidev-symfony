{% extends 'security/base-front-login.html.twig' %}

{% block body %}
    <h1 style="color: #d39e00">Connexion!</h1>

    <form action="{{ path('security_login') }}" method="post">
        <div class="form-group">
            <input placeholder="username..." required name="_username" type="text" class="form-control" >
        </div>
        <div class="form-group">
            <input placeholder="Mot de passe..." required name="_password" type="password" class="form-control" >
        </div>
        <div>
            <button type="submit" class="btn btn-success">Connexion!</button><br>
          {#   <a href="{{ path('staff_list') }}">se connecter entant qu'administrateur !</a>#}
        </div>

    </form>
{% endblock %}